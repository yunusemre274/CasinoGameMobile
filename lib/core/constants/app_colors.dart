import 'package:flutter/material.dart';

// App Color Palette - Dark/Neutral theme with controlled accents
class AppColors {
  // Primary backgrounds
  static const Color background = Color(0xFF1A1A2E);
  static const Color surface = Color(0xFF16213E);
  static const Color surfaceLight = Color(0xFF1F2B47);
  static const Color card = Color(0xFF222B45);

  // Text colors
  static const Color textPrimary = Color(0xFFF5F5F5);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color textMuted = Color(0xFF6B7280);

  // Accent colors
  static const Color accent = Color(0xFF6366F1);
  static const Color accentLight = Color(0xFF818CF8);
  static const Color accentDark = Color(0xFF4F46E5);

  // Status colors
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color danger = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Game-specific colors
  static const Color money = Color(0xFF10B981);
  static const Color health = Color(0xFFEF4444);
  static const Color hunger = Color(0xFFF59E0B);
  static const Color happiness = Color(0xFFEC4899);
  static const Color level = Color(0xFF8B5CF6);

  // Button colors
  static const Color buttonPrimary = Color(0xFF6366F1);
  static const Color buttonSecondary = Color(0xFF374151);
  static const Color buttonDisabled = Color(0xFF4B5563);

  // Border colors
  static const Color border = Color(0xFF374151);
  static const Color borderLight = Color(0xFF4B5563);

  // Shadow
  static const Color shadow = Color(0x40000000);
}
